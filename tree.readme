Fractal.readme

tree.html draws a fractal based tree configured by these 8 variables:

// Set these variables in the tree.html file and open it to see your tree.

var level = 12;  // Level of recursion
var initialAngle = -90; // Initial angle of the trunk
var length = 180; // Length of the first stem
var width = 21; // Width of the first stem
var leftAlpha = 1.2; 
var rightAlpha = 1.2;
var leftAngle = 25; // Left delta
var rightAngle = 25; // Right delta
